<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closed_Won_Cannot_be_Edit</fullName>
    <active>false</active>
    <description>*can&apos;t activate because this fights with our closed won process. Wait till new verification stage</description>
    <errorConditionFormula>AND(
NOT(contains( $Profile.Name, &quot;Admin&quot;)),
NOT(contains( $Profile.Name, &quot;Finance&quot;)),
ISPICKVAL(StageName, &quot;7W - Closed Won&quot;),
TEXT(PRIORVALUE(StageName)) = &apos;7W - Closed Won&apos;
)</errorConditionFormula>
    <errorMessage>Opportunity cannot be edited because the Opportunity is closed.</errorMessage>
</ValidationRule>
