<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Primary_Contact__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The Contract Signer. Note this will auto-populate to the Primary Contact on the Opportunity if left blank.</description>
    <externalId>false</externalId>
    <inlineHelpText>The Contract Signer. Note this will auto-populate to the Primary Contact on the Opportunity if left blank.</inlineHelpText>
    <label>Signer</label>
    <referenceTo>Contact</referenceTo>
    <relationshipLabel>Opportunities</relationshipLabel>
    <relationshipName>Opportunities</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
